import { Then, When } from 'cucumber';

When('client creates a GET request', function(callback) {
  callback(null, 'pending');
});

Then('retrieve all character details', function(callback) {
  callback(null, 'pending');
});
